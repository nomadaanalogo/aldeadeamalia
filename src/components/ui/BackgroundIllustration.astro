---
interface Props {
  src: string;
  class?: string;
}

const { src, class: className = '' } = Astro.props;
---

<img src={src} alt="" aria-hidden="true" class={`pointer-events-none select-none ${className}`} />
